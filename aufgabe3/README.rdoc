# Setup
* create secret key with "bundle exec rake secret" and set it in /config/secrets.yml
* set smtp logins in config/environments/

##

* public key in `authorized_keys`
* `ssh root@localhost -p 22`
